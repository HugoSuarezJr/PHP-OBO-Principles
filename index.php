<?php

function register(FirstName $first, LastName $last, Age $age, EmailAddress $email, Password $password){
    // Be considerate when making value objects. Only use when very clearly is beneificial.
}